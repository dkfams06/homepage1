# 관리자 워크스페이스

## 1. 현재 범위

로그인 전 UI 검토 단계다. `/ko/admin`과 `/en/admin`, `/zh/admin`, `/ja/admin`에서 제한 없이 **샘플 데이터**를 CRUD한다. 실제 개인정보를 입력하지 않는다. 실제 홈페이지 사전·시술·블로그·상담 접수 데이터는 변경하지 않는다.

| 경로 | 기능 |
| --- | --- |
| `/[locale]/admin` | 저장소 기반 항목 수, 미처리 문의 수, 콘텐츠 상태, 최근 변경 이력 |
| `/[locale]/admin/users` | 유저 CRUD, 역할 배정, 역할 CRUD 및 리소스별 read/create/update/delete 권한표 |
| `/[locale]/admin/service` | 시술 제목·슬러그·분류·요약·본문·이미지 경로·게시 상태 관리 |
| `/[locale]/admin/blog` | 글 제목·슬러그·분류·요약·본문·이미지 경로·게시 상태 관리 |
| `/[locale]/admin/inquire` | 문의 내용·이메일·담당자·우선순위·처리 상태·내부 메모 관리 |

공통 목록에는 검색, 상태 필터, 정렬, 페이지 크기, 페이지 이동, 선택 삭제, 상세 조회와 편집 팝업이 있다. 삭제는 확인을 거친다. 로딩·빈 목록·저장 오류·중복 항목도 표시한다. 이미지 경로는 텍스트 필드이며 파일 업로드 기능은 아니다.

## 2. 구조와 디자인

- 공개 사이트는 `src/app/[locale]/(site)/`, 관리자는 `src/app/[locale]/admin/`에 둔다. 기존 공개 URL은 그대로 유지한다.
- 공통 루트 레이아웃은 언어·폰트·문서만 담당한다. 관리자에는 공개 사이트의 헤더·푸터·상담 플로팅 버튼이 나오지 않는다.
- 기존 아이보리·블러시·로즈 토큰을 유지하면서 사이드바, 표, 상태 배지 중심의 밀도 높은 관리 화면을 사용한다. 모바일은 가로 메뉴와 표 내부 스크롤을 사용한다.
- 모든 라벨·상태·오류·접근성 문구는 네 언어 사전의 `admin`에 동기화한다.
- 관리자 메타데이터는 `noindex, nofollow`다. 이는 인증이나 보안 통제가 아니다.

## 3. 데이터와 TanStack Query

- 저장소 어댑터: `src/lib/admin/repository.ts`. 화면에서 localStorage를 직접 조작하지 않는다.
- 현재 저장 키: `lian-admin-demo:v1:{locale}`. **브라우저·출처·언어별 독립 데이터**이며 서버 공유나 번역 콘텐츠 연결이 아니다. 새로고침 후 유지되지만 사이트 데이터 삭제 시 사라진다.
- QueryClient는 관리자 Provider 생명주기 동안 유지한다. staleTime 30초, gcTime 10분, 자동 재시도 없음. 로컬 어댑터라 오프라인에서도 동작한다.
- 키: `['admin-demo', locale, 'overview']`, `[..., entity, 'list', filters]`, `[..., entity, 'detail', id]`.
- `useQuery`로 목록·상세·집계를 조회하고 `useMutation`으로 저장·삭제한다. 저장 성공 이후 언어 범위 캐시를 무효화하여 목록·연결 역할·대시보드가 함께 갱신된다. 다른 탭의 storage 이벤트도 캐시를 갱신한다.
- 저장 실패를 성공으로 표시하지 않는다. 손상된 저장소를 자동 초기화하거나 덮어쓰지 않는다.
- 역할 삭제 시 유저 역할을 비우고 유저 삭제 시 담당 문의 연결을 비운다. 변경 이력은 최근 50개까지 보존한다. 데모 이력은 보안 감사 로그가 아니다.

## 4. RBAC와 운영 전환 경계

현재 권한표는 **편집·저장만 하는 정책 모델**이다. 로그인 사용자가 없으므로 버튼·화면·API 접근 제한을 적용하지 않는다. 모든 역할을 삭제하거나 권한 없이 편집하는 것도 데모에서는 허용된다.

운영 연동 순서는 다음과 같다.

1. 로그인·세션·서버 데이터베이스를 연결한다.
2. 저장소 어댑터를 인증된 API로 교체하고 매 요청마다 서버에서 리소스/행위 권한을 검증한다. 클라이언트 버튼 숨김만으로 RBAC를 구현하지 않는다.
3. 사용자/역할과 다국어 콘텐츠 모델을 분리하고 공통 콘텐츠 ID와 번역 레코드 관계를 정의한다. 현재 언어별 로컬 샘플은 운영 DB로 자동 승격하지 않는다.
4. 실제 홈페이지 게시·문의 접수·파일 업로드를 연결한다. 초대/계정 관리, 유효성 검증, 마지막 관리자 보호, 불변 감사 로그, 개인정보 보존/삭제 정책을 함께 설계한다.
5. 서버 권한 거부·만료 세션·동시 수정·페이지 캐시 재검증까지 검증한 뒤 실제 데이터를 사용한다.

## 5. 검증

`npm run check:i18n`, `npx tsc --noEmit`, `npm run lint`, `npm run test:unit`, `npm run build`, `npm run test:e2e`.

관리자 단위 테스트는 CRUD 영속성, 언어 격리, 검색/페이지, 참조 정리, 유효성, 저장 실패를 검증한다. 브라우저 테스트는 네 언어와 데스크톱/모바일에서 사용자 CRUD·팝업 포커스·서비스/블로그/문의 CRUD·권한표·오류 표시를 검증한다.
