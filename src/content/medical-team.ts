/** Only add hospital-verified credentials. Empty lists intentionally show a pending notice. */
export const chiefDoctorId = "doctor-1";
export const profileFields = ["education", "career", "publications", "liveSurgery"] as const;
